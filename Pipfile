[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
networkx = "==2.5"
pytest = ">=3.0"

[dev-packages]

[requires]
python_version = "3.13"

[scripts]
test = "pytest"
